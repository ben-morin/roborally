# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base@1.5.2             # Packages every Meteor app needs to have
ecmascript@0.19.0             # Compiles app .js as ES modules (enables meteor.mainModule)
mongo@2.5.0                   # The database Meteor supports right now
blaze-html-templates          # Compile .html files into Meteor Blaze views
reactive-var@1.0.13           # Reactive variable for tracker
reactive-dict@1.3.2           # Reactive key-value dict for tracker
tracker@1.3.4                 # Meteor's client-side reactive programming library

standard-minifier-js@3.2.0    # JS minifier run for production mode
shell-server@0.7.0            # Server-side component of the `meteor shell` command

# Under Rspack these no longer hot-patch Blaze templates — every edit triggers a
# full reload. They are still worth keeping: without them a .html edit reloads
# twice (the first serving stale content). Measured 2026-07-28. debugOnly, so
# they never reach production.
hot-module-replacement@0.6.0
blaze-hot

mizzao:user-status@2.0.0-rc.2
mizzao:timesync@1.0.0-beta.2
linto:fontawesome

jquery@3.0.2
email@3.2.0

accounts-password@3.3.1
ben-morin:accounts-ui-roborally
ddp-rate-limiter@1.3.0        # server/rateLimits.js; was already in the bundle transitively

ostrio:flow-router-extra

quave:synced-cron@2.4.0
rspack@1.2.0
